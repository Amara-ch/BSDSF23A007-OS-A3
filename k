#include "shell.h"
#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("Shell started\n");
    while (1) {
        // Call your shell functions, e.g., read_cmd() and execute()
    }
    return 0;
}
